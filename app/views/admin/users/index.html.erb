<div class="row">
  <%= render partial: 'admin/shared/navtab' %>
  <div class="user_list">
    <div class="col-md-8">
      <table class="table table-striped table-bordered">
        <thead>
          <thead>
              <tr>
                <th scope="col"><%= sort_link(@ransack, :id) %></th>
                <th scope="col"><%= sort_link(@ransack, :name) %></th>
                <th scope="col"><%= sort_link(@ransack, :role) %></th>
              </tr>
            </thead>
            <tbody>
              <% @users.each do |user| %>
                <tr>
                  <th><%= user.id %></th>
                  <td><%= user.name %></td>
                  <td><%= user.role %></td>
                </tr>
              <% end %>
            </tbody>
        </thead>
      </table>
    </div>
    <div class="col-md-4">
      <h3>Search</h3>
      <%= search_form_for @ransack , url: admin_users_path do |f| %>

        <div>
          <%= f.label :id_cont %>
          <%= f.search_field :id_cont %>
        </div>

        <div>
          <%= f.label :name_ %>
          <%= f.search_field :name_cont %>
        </div>

        <div>
          <%= f.label :role_cont %>
          <%= f.search_field :role_cont  %>
        </div>

        <div>
          <%= f.submit '搜尋', class: 'btn btn-primary' %>
          <%= button_tag "重置", type: :reset, class: 'btn btn-default' %>
        </div>

      <% end %>
    </div>
  </div>
</div>